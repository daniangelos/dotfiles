if-shell "test -f ~/.bubblegum.snapshot" "source ~/.bubblegum.snapshot"

setw -g mode-keys vi
# Toggle mouse on with ^B m
bind m \
  set -g mouse on \;\
  display 'Mouse: ON'

# Toggle mouse off with ^B M
bind M \
  set -g mouse off \;\
  display 'Mouse: OFF'


